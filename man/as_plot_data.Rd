% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_plot_data.R, R/plot.check_na_gapsize.R,
%   R/plot.check_na_timing.R
\name{as_plot_data}
\alias{as_plot_data}
\alias{as_plot_data.default}
\alias{as_plot_data.check_na_gapsize}
\alias{as_plot_data.check_na_timing}
\title{Prepare a Check Result for Plotting}
\usage{
as_plot_data(x, ...)

\method{as_plot_data}{default}(x, ...)

\method{as_plot_data}{check_na_gapsize}(x, ..., measure = c("occurrence", "total"))

\method{as_plot_data}{check_na_timing}(x, ...)
}
\arguments{
\item{x}{A check object.}

\item{...}{Additional arguments passed to methods.}

\item{measure}{For \code{as_plot_data.check_na_gapsize()}: which count to put on
the y axis — \code{"occurrence"} (number of gaps) or \code{"total"} (missing frames).}
}
\value{
A data frame classed for the corresponding plot method (for example
\code{anivis_check_na_timing}).
}
\description{
Coerces a check object — such as the result of \code{check_na_timing()} (from the
anicheck package) — into the
plot-ready data frame that its \code{plot()} method draws from. This is the
staging step that sits between the diagnostic computation (the \verb{check_*()}
function) and the ggplot: it reshapes the natural, analysis-friendly columns
of the check into the exact aesthetic contract a geom needs (a single
grouping factor, ordered fill levels, the right level ordering, and so on).
}
\details{
It is the anivis analog of \code{data_plot()} in the \pkg{see} package. Most users
never call it directly — \code{plot()} calls it for you. Reach for it when you
want the precise rows a plot is built from, to inspect them or to assemble a
custom chart by hand.

Methods exist for check objects with a single canonical plot (one object
class maps to one figure). Object types that can be plotted several different
ways — an aniframe, which feeds both \code{\link[=plot_trajectory]{plot_trajectory()}} and
\code{\link[=plot_timeseries]{plot_timeseries()}} — deliberately do not have a single method, since there
would be nothing for it to dispatch on to choose between those shapes.

\code{as_plot_data.check_na_gapsize()} turns the gap-size table from
\code{check_na_gapsize()} into a tidy bar-chart frame: one row per (group, gap
size) with the chosen \code{measure} as \code{value}, \code{gap_size} an ascending factor so
every bar position lines up across panels, and the varying grouping columns
collapsed into a single \code{group} factor. Returns a frame classed
\code{anivis_check_na_gapsize}.

\code{as_plot_data.check_na_timing()} turns the gap table from \code{check_na_timing()}
into the frame its strip plot is drawn from. Each gap becomes a tile centred
on the gap with a width spanning it (plus one frame step, so single-frame gaps
stay visible); the grouping columns that actually vary collapse into a single
\code{group} factor, reversed so the first group reads at the top. The full set of
groups (including gap-free ones) and the time range ride along as attributes
so the plot can show every row across the whole timeline. Returns a frame
classed \code{anivis_check_na_timing}.
}
\seealso{
\code{\link[=plot.check_na_timing]{plot.check_na_timing()}}
}
