% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/animovement_scales.R
\name{animovement_palette}
\alias{animovement_palette}
\title{Per-group colour palette for an aniframe}
\usage{
animovement_palette(data, palette = "Dark 3", single_hue = "#3A6FB0")
}
\arguments{
\item{data}{An aniframe.}

\item{palette}{Name of a qualitative palette accepted by
\code{\link[grDevices:hcl.colors]{grDevices::hcl.colors()}}. Default \code{"Dark 3"}.}

\item{single_hue}{Colour used when neither \code{what} nor \code{when} has more than
one level.}
}
\value{
A named character vector of hex colours.
}
\description{
Builds a named character vector mapping trajectory group keys to colours.
The colour scheme adapts to how many \code{what} and (non-time) \code{when} levels
the aniframe has:
}
\details{
\itemize{
\item \strong{single} (one of each, or none): a single hue.
\item \strong{what}  (multiple \code{what}, one \code{when}): one qualitative hue per \code{what}
level.
\item \strong{when}  (one \code{what}, multiple \code{when}): one qualitative hue per \code{when}
level.
\item \strong{matrix} (multiple of both): one hue per \code{what} level, then a
lighter-to-darker shade of that hue per \code{when} level.
}

Names of the returned vector match the group keys returned by the internal
grouping logic — the redundant axis is dropped when only one side varies.
}
