% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.anievent.R
\name{plot_ethogram}
\alias{plot_ethogram}
\alias{plot_ethogram.default}
\title{Plot an Ethogram}
\usage{
plot_ethogram(data, ...)

\method{plot_ethogram}{default}(data, ..., mode = c("light", "dark"))
}
\arguments{
\item{data}{An anievent object.}

\item{...}{Additional arguments (currently unused).}

\item{mode}{Either \code{"light"} (default) or \code{"dark"}; passed to
\code{\link[=theme_animovement]{theme_animovement()}}.}
}
\value{
A ggplot object.
}
\description{
Creates a ggplot ethogram of state bouts from an anievent. Each behaviour
\code{label} becomes a row on the y axis, bouts are coloured by \code{label}, and
the time axis is labelled with \code{unit_time} from metadata.
}
\details{
The plot is faceted automatically:
\itemize{
\item Multiple \code{channel}s → one facet row per channel, with free y-scales so
each channel's labels keep their own ranking.
\item Multiple identities (any \code{variables_what} column with more than one
level) → one facet column per identity. When both \code{channel} and
identity vary, a \code{facet_grid()} is used; otherwise \code{facet_wrap()}.
}
}
