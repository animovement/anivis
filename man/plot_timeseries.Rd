% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_timeseries.R
\name{plot_timeseries}
\alias{plot_timeseries}
\alias{plot_timeseries.default}
\title{Plot a Variable as a Time Series}
\usage{
plot_timeseries(data, ...)

\method{plot_timeseries}{default}(
  data,
  variable = NULL,
  ...,
  layout = c("inline", "facet"),
  mode = c("light", "dark"),
  palette = "Dark 3"
)
}
\arguments{
\item{data}{An aniframe object.}

\item{...}{Additional arguments (currently unused).}

\item{variable}{Name(s) of the column(s) to plot on the y axis — a character
vector. Required (no auto-detection). Several names draw a stacked panel
per variable.}

\item{layout}{Either \code{"inline"} (default, all groups in one panel) or
\code{"facet"} (one panel per group).}

\item{mode}{Either \code{"light"} (default) or \code{"dark"}; passed to
\code{\link[=theme_animovement]{theme_animovement()}}.}

\item{palette}{Name of a qualitative palette accepted by
\code{\link[grDevices:hcl.colors]{grDevices::hcl.colors()}}; controls the hue family across groups.}
}
\value{
A ggplot object for a single variable, or a stacked patchwork for
several.
}
\description{
Plots one or more numeric variables from an aniframe against \code{time}, with one
line per trajectory group (every \code{variables_what} column and every non-time
\code{variables_when} column in the metadata). Use it for any per-frame measure —
\code{speed}, \code{acceleration}, a confidence score, a temperature channel, or any
other derived column.
}
\details{
Pass several names to \code{variable} to draw a panel per variable, \strong{vertically
stacked} with a shared x axis and a single shared legend (via \code{\link[=plots]{plots()}});
stacking makes it easy to compare their shape over time. A single variable
returns a plain ggplot.

The \code{layout} argument chooses how groups are arranged, mirroring
\code{\link[=plot_events]{plot_events()}}:
\itemize{
\item \code{"inline"} (default): all groups share one panel as coloured lines, with a
legend when there is more than one group.
\item \code{"facet"}: each group gets its own panel — \code{facet_grid()} when both an
identity and a (non-time) condition vary, otherwise \code{facet_wrap()} with the
panels stacked in rows so the shared x axis lines up.
}

Colours come from \code{\link[=palette_animovement]{palette_animovement()}}, matching \code{\link[=plot_trajectory]{plot_trajectory()}}. The
x axis uses the same time-unit handling as \code{\link[=plot_events]{plot_events()}} — \code{HH:MM:SS} for
true time units, raw frames otherwise.
}
