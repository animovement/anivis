% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plots}
\alias{plots}
\title{Arrange multiple plots side by side}
\usage{
plots(
  ...,
  n_rows = NULL,
  n_columns = NULL,
  guides = NULL,
  axes = NULL,
  axis_titles = NULL,
  tags = FALSE,
  tag_prefix = NULL,
  tag_suffix = NULL,
  tag_sep = NULL,
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  theme = NULL
)
}
\arguments{
\item{...}{Plots to combine, or a single \code{list} of plots.}

\item{n_rows, n_columns}{Number of rows / columns in the layout. By default
patchwork chooses a roughly square grid.}

\item{guides}{How to treat legends: \code{"collect"} to pool identical legends
into one, \code{"keep"} to leave them in place. Passed to
\code{\link[patchwork:wrap_plots]{patchwork::wrap_plots()}}.}

\item{axes, axis_titles}{How to treat duplicated axes / axis titles:
\code{"collect"}, \code{"collect_x"}, \code{"collect_y"} to share them across aligned
panels, or \code{"keep"} (default). Passed to \code{\link[patchwork:wrap_plots]{patchwork::wrap_plots()}}.}

\item{tags}{Sub-figure tags. \code{TRUE} labels panels \code{A}, \code{B}, \code{C}, ...; a
character vector such as \code{c("1", "2")} or \code{"i"} chooses another sequence;
\code{FALSE} (default) adds none.}

\item{tag_prefix, tag_suffix, tag_sep}{Strings placed before / after each tag,
and the separator between nested tag levels.}

\item{title, subtitle, caption}{Overall annotations for the combined figure.}

\item{theme}{A ggplot2 theme applied to the annotations (e.g. the title).}
}
\value{
A patchwork object.
}
\description{
A thin convenience wrapper around \pkg{patchwork} for composing several
plots into a single figure with optional sub-figure tags, a shared legend,
and an overall title / subtitle / caption. Vendored from the see package so
anivis plots can be combined without reaching for patchwork directly.
}
\examples{
library(ggplot2)
p1 <- ggplot(mtcars, aes(mpg, wt)) + geom_point()
p2 <- ggplot(mtcars, aes(factor(cyl))) + geom_bar()
plots(p1, p2, tags = TRUE)

}
