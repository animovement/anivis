% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_ethogram.R
\name{geom_ethogram}
\alias{geom_ethogram}
\title{Draw an ethogram from an anievent}
\usage{
geom_ethogram(
  mapping = NULL,
  data = NULL,
  ...,
  height = 0.7,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping}{Aesthetic mapping. Merged on top of the defaults, so
you only need to supply the aesthetics you want to override.}

\item{data}{A data frame (typically an \code{anievent}). If \code{NULL}, inherits
from the parent \code{ggplot()} call. Either way, rows with
\code{type != "state"} are dropped from this layer.}

\item{height}{Vertical extent of each bout bar, in y-axis units
(1 = the spacing between adjacent label rows). Default 0.7.}

\item{na.rm, show.legend, inherit.aes, ...}{Passed to \code{\link[ggplot2:layer]{ggplot2::layer()}}.}
}
\value{
A ggplot2 layer.
}
\description{
A ggplot2 layer that renders state bouts of an \code{\link[aniframe:anievent]{aniframe::anievent()}} as
rectangles spanning each bout's \code{start} -> \code{stop} on the x axis, with one
row per \code{label} on the y axis. Point events (\code{type == "point"}) are
filtered out of the layer's data before drawing.
}
\details{
The default mapping is only \code{aes(xmin = start, xmax = stop)} — every
other aesthetic is up to the caller. Common patterns:
\itemize{
\item \code{aes(fill = label)} — colour each bout's interior by behaviour.
\item \code{aes(colour = label)} — outline each bout (keeps sub-pixel-wide
bouts visible at any zoom).
\item \code{aes(y = label)} (or \code{y = channel}, \code{y = subject}, ...) — spread
bouts across rows. With no \code{y} mapping all bouts collapse onto a
single line.
\item \code{+ ggplot2::facet_wrap(~ channel, scales = "free_y", ncol = 1)} —
split channels into their own panels.
}

Opinionated wrappers (themes, colour scales, sensible defaults) are
intended to live one layer up; this geom is deliberately bare.

This is a thin wrapper around \link[ggplot2:GeomRect]{ggplot2::GeomRect}: a subclass adds a
\code{setup_data} step that derives \code{ymin} / \code{ymax} from \code{y} + \code{height}, so
a discrete y scale keeps the label rows ranked correctly (including
under per-panel \code{scales = "free_y"}).
}
